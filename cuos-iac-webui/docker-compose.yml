services:
  cuos-iac-webui:
    image: "ghcr.io/cuos-dev/cuos-iac-webui:latest"
    container_name: cuos-iac-webui
    volumes:
      - /var/run/cuos.sock:/var/run/cuos.sock
      - /system.json:/system.json:ro
    ports:
      - "8030:3000"
    restart: always
#    profiles:
#      - dev

